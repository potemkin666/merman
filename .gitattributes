# Ensure batch files always have Windows CRLF line endings on checkout,
# regardless of the contributor's platform or git config.
*.bat text eol=crlf

# Ensure shell scripts always have Unix LF line endings on checkout.
*.sh  text eol=lf
